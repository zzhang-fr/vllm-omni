# Stage config for Lingbot World Fast online serving.

stage_args:
  - stage_id: 0
    stage_type: diffusion
    runtime:
      process: true
      devices: "0"
    final_output: true
    final_output_type: image
    engine_args:
      model_class_name: LingbotWorldFastPipeline
      dtype: bfloat16
      model_config:
        patch_size: [1, 2, 2]
        vae_stride: [4, 8, 8]
        latent_frames_per_chunk: 3
        max_area: 399360  # 480 * 832
